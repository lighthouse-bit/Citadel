import{useState}from'react';import{useNavigate,useSearchParams}from'react-router-dom';import{authAPI}from'../../services/api';import toast from'react-hot-toast';
export default function AdminResetPassword(){const[params]=useSearchParams(),navigate=useNavigate(),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();if(password!==confirm)return toast.error('Passwords do not match');try{setLoading(true);await authAPI.resetAdminPassword({token:params.get('token'),password});toast.success('Password reset. Sign in with your new password.');navigate('/admin/login');}catch(error){toast.error(error.response?.data?.error||'Reset failed');}finally{setLoading(false);}};return <div className="min-h-screen bg-stone-900 flex items-center justify-center p-4"><form onSubmit={submit} className="bg-white rounded-2xl p-8 w-full max-w-md space-y-4"><h1 className="text-2xl font-serif">Reset admin password</h1><p className="text-sm text-stone-500">Use at least 12 characters.</p><input type="password" minLength="12" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="w-full border rounded-lg px-4 py-3"/><input type="password" minLength="12" required value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm password" className="w-full border rounded-lg px-4 py-3"/><button disabled={loading} className="w-full bg-stone-900 text-white rounded-lg py-3">{loading?'Resetting…':'Reset password'}</button></form></div>;}
